## Change Contract: Frontend UI Update

### Change Type
Feature update (non-sprint change to completed platform)

### Features
1. 侧边栏导航改造
   - 菜单栏从顶部移至左侧侧边栏
   - 支持收起/展开侧边栏（图标模式 vs 完整模式）
   - 固定侧边栏高度为浏览器视口高度100vh
   - 支持响应式适配（桌面/平板/手机断点）

2. 国际化改造
   - 全局默认语言改为中文
   - 保留中英文切换按钮（位于侧边栏或内容区右上角）
   - 语言偏好存储在localStorage

### Success Criteria (black-box verifiable)
1. `http://localhost:5173` 打开后，菜单显示在页面左侧垂直排列，非顶部横排
2. 侧边栏右上角有收起/展开按钮，点击后菜单收起为仅图标（约48px宽），再次点击恢复完整模式（约220px宽）
3. 侧边栏高度始终为浏览器视口高度，内容区域独立滚动
4. 菜单项显示中文文字（如"物料管理"而非"Material Management"）
5. 侧边栏底部或内容区右上角有语言切换按钮，显示"EN/中文"或类似标签
6. 点击语言切换后，页面菜单文字立即切换语言，刷新后保持选择
7. 在1024px宽度以下，侧边栏自动收起为图标模式或显示汉堡菜单按钮

### Scope
- `frontend/index.html` — 调整布局结构
- `frontend/styles.css` — 新增侧边栏样式，移除顶部导航横排样式
- `frontend/app.js` — 新增侧边栏收起状态、国际化状态和切换逻辑

### Evaluator Steps
1. 打开 `http://localhost:5173`，确认菜单在左侧垂直排列而非顶部
2. 点击侧边栏收起按钮，确认菜单收起为图标模式，展开后恢复
3. 调整浏览器高度至小于内容区高度，确认侧边栏高度不变，内容区滚动
4. 确认所有菜单项显示中文
5. 点击语言切换，确认菜单文字变为英文，再次点击变回中文
6. 刷新页面，确认语言选择被记住
7. 调整浏览器宽度至1024px以下，确认侧边栏自动调整或出现移动端菜单

---

APPROVED